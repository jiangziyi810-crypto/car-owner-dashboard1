# 车主运营看板：GitHub 数据更新说明

以后 `index.html` 不需要修改。看板会在打开时自动读取仓库根目录下的 5 个 Excel；文件名使用下表中的中文基础名。

| Excel 基础名 | 对应原始数据 | 更新模块 |
|---|---|---|
| `车主关系绑定数据` | 车主关系绑定数据 | PPL 全国/省区 |
| `推荐复购聚合表` | 推荐/复购商机聚合表 | 推荐、复购达成 |
| `任务圈选下发聚合表` | 任务圈选下发聚合表 | 圈选完成率 |
| `复购渠道明细表` | 复购商机渠道明细 | 复购渠道模块 |
| `推荐订单占比` | 推荐订单 by 区域明细 | 推荐订单气泡图 |

支持 `.xlsx` 与 `.xls`。文件名末尾的 Excel 自动编号会被忽略，例如 `推荐订单占比（10）.xlsx` 或 `推荐订单占比(10).xlsx` 都会被识别为“推荐订单占比”。

> 若仓库中同时存在无编号文件和带编号文件，优先读取无编号文件，例如优先读取 `推荐订单占比.xlsx`。

## 日常更新

1. 下载最新业务 Excel。
2. 将文件改成上表的中文基础名并保留 Excel 后缀；例如 `推荐订单占比.xlsx`。如果下载后已有 `（10）` 这类编号后缀，也可以直接上传。
3. GitHub 仓库首页 → **Add file → Upload files**。
4. 上传文件并 **Commit changes**。
5. 等 GitHub Pages 发布几十秒，刷新看板。页面会绕过浏览器缓存重新读取 Excel。

如果某一天只更新推荐订单，只上传 `推荐订单占比.xlsx`（或同名带编号后缀的文件）即可；其他 4 个文件不用动。

## 统计周期（自动更新）

看板每次打开时都会自动读取仓库根目录的 `dashboard-config.json`，并更新页面上的统计周期文字；不需要修改 `index.html`。

更新数据时，同时在 GitHub 网页编辑该文件并提交即可：

```json
{
  "recommendationPeriod": "8.16—8.30",
  "channelPeriod": "8.24—8.30"
}
```

- `recommendationPeriod`：推荐订单气泡图的数据周期。
- `channelPeriod`：复购渠道模块的数据周期。

提交 Excel 和 `dashboard-config.json` 后，等待 GitHub Pages 发布几十秒并刷新看板；页面会自动显示新周期。

## 兼容说明

- 保留看板原来的“更新数据”按钮，可继续本地上传 Excel 做临时预览。
- GitHub Pages 上打开时优先读取固定中文基础名的线上 Excel，并兼容其末尾的数字编号。
- 某个 Excel 缺失或读取失败时，仅该模块沿用 HTML 内置数据，不会让整个看板打不开。
- 已兼容部分业务导出 Excel 的 worksheet dimension 错写为 `A1` 的问题。
- 推荐订单合计占比按各区域“推荐订单量合计 ÷ 净锁单量合计”计算，避免导出表合计行分母口径异常。
